<%= simple_form_for client do |f| %>
  <div class="form-inputs">
    <%= f.input :name, autofocus: true, :input_html => {:minlength => 1} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Save" %>
  </div>
<%end%>
